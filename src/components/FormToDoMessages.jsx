import { React } from 'react';

const FormToDoMessages = ({ message }) => {
  return (
    <div className="FormToDoMessages">
      <h2>{message}</h2>
    </div>
  );
};

export default FormToDoMessages;
